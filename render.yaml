databases:
  - name: foodon # Give your database a descriptive name
    databaseName: foodon  # Usually the same as the name
    user: foodon # Create a specific user for your database
    plan: free # or 'standard-0' if you need more resources

services:
  - type: web
    name: foodon # Name your web service
    runtime: ruby 
    plan: free # Or a paid plan if you need more resources
    buildCommand: "./bin/render-build.sh"
    # Pre-deploy Command (Only for Paid Plans):
    # preDeployCommand: "bundle exec rails db:migrate" 
    startCommand: "bundle exec rails server -p 3000" # Start Rails server on port 3000
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: foodon # Reference your database name
          property: connectionString
      - key: RAILS_MASTER_KEY
        generateValue: true  # Automatically generate a secure master key
      - key: RAILS_ENV 
        value: production
      - key: WEB_CONCURRENCY
        value: 2
